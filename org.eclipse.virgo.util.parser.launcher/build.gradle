apply plugin: 'osgi'

jar {
    manifest {
        version = project.version
        name = 'Virgo Launcher Parser'
        instruction 'Import-Package', '*'
        instruction 'Export-Package', '*'
        attributes( 'Bundle-SymbolicName': 'org.eclipse.virgo.util.parser.launcher')
        attributes( 'Built-By': "Gradle ${gradle.gradleVersion}" )
    }
}
